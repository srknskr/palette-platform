# Palette Mobile

The mobile client will use Kotlin Multiplatform for shared networking, domain models, and repositories. Android UI will use Jetpack Compose and iOS UI will use SwiftUI.

Planned structure:

```text
mobile/
├── shared/
├── androidApp/
└── iosApp/
```
