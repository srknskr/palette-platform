# Palette Web

The Vue 3 + TypeScript client is planned after the backend discovery and identity APIs are stable. Node.js will be used for frontend tooling only; the application backend remains Spring Boot.
