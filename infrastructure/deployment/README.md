# Deployment

Production deployment configuration will be added after the API and security milestones. It must keep secrets outside the repository, use managed TLS, run database migrations once per release, expose readiness/liveness probes, and provide automated rollback.
